<!-- Top Navigation -->
<nav class="bg-white border-b border-gray-200 shrink-0 z-20 relative">
  <div class="h-14 flex items-center justify-between px-4">
    <div class="flex items-center space-x-4 md:space-x-6 min-w-0">
      <!-- Logo -->
      <div class="flex items-center space-x-2 cursor-pointer shrink-0" onclick="app.navigate('dashboard')">
        <div class="w-6 h-6 bg-black rounded flex items-center justify-center text-white font-bold text-xs">P</div>
        <span class="font-semibold tracking-tight text-sm hidden sm:inline">
          <%= app_name %>
        </span>
      </div>

      <div class="h-4 w-px bg-gray-300 hidden sm:block shrink-0"></div>

      <!-- Context Switcher -->
      <div class="relative group min-w-0">
        <button id="context-trigger"
          class="flex items-center space-x-2 text-sm font-medium hover:bg-gray-50 px-2 py-1 rounded transition-colors max-w-full">
          <span id="current-podcast-name" class="truncate max-w-[120px] sm:max-w-xs">Loading...</span>
          <span class="text-gray-400 text-xs shrink-0">▼</span>
        </button>
        <!-- Dropdown -->
        <div
          class="absolute top-full left-0 mt-1 w-56 bg-white border border-gray-200 shadow-lg rounded-md hidden group-hover:block py-1 z-50">
          <div class="px-3 py-1.5 text-xs font-mono text-gray-400 uppercase tracking-wider">Switch Org/Project</div>
          <div id="podcast-list-container"></div>
        </div>
      </div>

      <!-- Desktop Nav Links -->
      <div class="hidden md:flex items-center space-x-1">
        <button onclick="app.navigate('dashboard')"
          class="px-3 py-1.5 text-sm font-medium text-gray-600 hover:text-black hover:bg-gray-50 rounded transition-colors">Home</button>
        <button onclick="app.navigate('plans')"
          class="px-3 py-1.5 text-sm font-medium text-gray-600 hover:text-black hover:bg-gray-50 rounded transition-colors">Plans</button>
        <button onclick="app.navigate('library')"
          class="px-3 py-1.5 text-sm font-medium text-gray-600 hover:text-black hover:bg-gray-50 rounded transition-colors">Library</button>
      </div>
    </div>

    <div class="flex items-center space-x-4 shrink-0">
      <!-- User Profile -->
      <div class="flex items-center space-x-2">
        <div
          class="w-8 h-8 rounded-full bg-gray-100 border border-gray-200 flex items-center justify-center text-xs font-mono">
          JD</div>
      </div>

      <!-- Mobile Menu Button -->
      <button onclick="app.toggleMobileMenu()" class="md:hidden p-2 text-gray-500 hover:text-black focus:outline-none">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
        </svg>
      </button>
    </div>
  </div>

  <!-- Mobile Menu (Hidden by default) -->
  <div id="mobile-menu"
    class="hidden md:hidden border-t border-gray-100 bg-gray-50 px-4 py-2 space-y-1 absolute w-full z-50 shadow-lg">
    <button onclick="app.navigate('dashboard')"
      class="block w-full text-left px-3 py-2 text-sm font-medium text-gray-600 hover:text-black hover:bg-gray-100 rounded transition-colors">Home</button>
    <button onclick="app.navigate('plans')"
      class="block w-full text-left px-3 py-2 text-sm font-medium text-gray-600 hover:text-black hover:bg-gray-100 rounded transition-colors">Plans</button>
    <button onclick="app.navigate('library')"
      class="block w-full text-left px-3 py-2 text-sm font-medium text-gray-600 hover:text-black hover:bg-gray-100 rounded transition-colors">Library</button>
  </div>
</nav>
